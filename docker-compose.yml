services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.cpu
    image: fbrl-dev
    volumes:
      - .:/workspace
      # Mount flodl source for path dependency
      - ../rdl:/rdl
      # Cache Rust build artifacts and registry between runs
      - cargo-registry:/root/.cargo/registry
      - cargo-git:/root/.cargo/git
    working_dir: /workspace
    stdin_open: true
    tty: true

volumes:
  cargo-registry:
  cargo-git:
